# Sprint 1 Sub-Plan — Project Scaffold + Directory Structure + Static Chat UI

> **How to read this document:** Tasks are in strict execution order.
> Each task has exactly one output. Complete and verify each task before starting the next.
> The AI IDE must execute each task exactly as written — no interpretation, no shortcuts.

---

### Task 1.1 — Initialize the Next.js project with TypeScript, Tailwind, and App Router

**Files involved:**
- `/` (project root — all scaffold files are created here by the command)

**Required input:**
- An empty or existing directory at the project root (pre-existing `.md` files and `.git/` are safe — the command adds files without removing them)

**Expected output:**
A working Next.js project scaffold with the following files present:
`package.json`, `tsconfig.json`, `tailwind.config.ts`, `postcss.config.mjs`,
`next.config.ts` *(will be replaced in Task 1.5)*, `app/page.tsx`, `app/layout.tsx`,
`app/globals.css`, `.gitignore` (generated by create-next-app)

**Success criterion:**
Run the following command in the project root:
```bash
npx create-next-app@latest . --typescript --tailwind --app --no-src-dir --eslint --import-alias "@/*"
```
Then run:
```bash
npm run dev
```
The terminal must show `✓ Ready in Xms` (or equivalent "ready" message) with no errors.
Opening `http://localhost:3000` must show the default Next.js welcome page.
`package.json` must contain `"next"` in `dependencies`.

---

### Task 1.2 — Verify that `.env.local` is listed in `.gitignore`

**Files involved:**
- `/.gitignore` (read and modify if needed)

**Required input:**
- Task 1.1 complete (`.gitignore` exists, generated by create-next-app)

**Expected output:**
`/.gitignore` contains the exact line `.env.local` (create-next-app includes it by default —
this task verifies it; if missing, add it).

**Success criterion:**
Run:
```bash
grep "\.env\.local" .gitignore
```
Output must be exactly `.env.local`. If the grep returns nothing, add `.env.local` on its
own line in `.gitignore` and re-run the grep until it passes.

---

### Task 1.3 — Create `.env.local` with the API key placeholder

**Files involved:**
- `/.env.local` (create new)

**Required input:**
- Task 1.2 complete (`.gitignore` is confirmed to list `.env.local`)

**Expected output:**
`/.env.local` exists at the project root with exactly this content:
```
ANTHROPIC_API_KEY=your-api-key-here
```

**Success criterion:**
Run both commands and confirm both expected outputs:
```bash
# 1. File exists and has the right content
cat .env.local
# Expected output: ANTHROPIC_API_KEY=your-api-key-here

# 2. Git does not track the file (R5 mitigation — critical)
git status
# Expected: .env.local must NOT appear in the output under "Changes" or "Untracked files"
# If it appears, Task 1.2 was not completed correctly — stop and fix .gitignore first.
```

---

### Task 1.4 — Create the required empty directory and file placeholders

**Files involved:**
- `/knowledge-base/.gitkeep` (create — makes the directory trackable by Git)
- `/lib/.gitkeep` (create)
- `/prompts/.gitkeep` (create)
- `/app/components/.gitkeep` (create)

**Required input:**
- Task 1.1 complete (project root exists)

**Expected output:**
Four directories exist at the project root: `/knowledge-base/`, `/lib/`, `/prompts/`,
`/app/components/`. Each contains an empty `.gitkeep` file to make them Git-trackable.
These directories will be populated with real content in Sprints 2–4.

**Success criterion:**
Run:
```bash
ls -d knowledge-base lib prompts app/components
```
All four directory names must appear in the output with no "No such file or directory" error.

---

### Task 1.5 — Write `next.config.js` with `outputFileTracingIncludes`

**Files involved:**
- `/next.config.js` (create new — replaces the `next.config.ts` generated by create-next-app)
- `/next.config.ts` (delete — replaced by the `.js` version for CommonJS compatibility with the config below)

**Required input:**
- Task 1.1 complete

**Expected output:**
`/next.config.js` exists with exactly this content (no additions, no modifications):
```js
/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    outputFileTracingIncludes: {
      '/api/chat': ['./knowledge-base/**/*.md'],
    },
  },
};

module.exports = nextConfig;
```

**Success criterion:**
Run:
```bash
npm run dev
```
The dev server must start without errors. Then run:
```bash
node -e "const c = require('./next.config.js'); console.log(JSON.stringify(c.experimental.outputFileTracingIncludes))"
```
Expected output:
```
{"/api/chat":["./knowledge-base/**/*.md"]}
```
If the output matches, the configuration is correctly registered.

---

### Task 1.6 — Configure `globals.css` with design tokens and Google Font import

**Files involved:**
- `/app/globals.css` (overwrite — replace the default Tailwind boilerplate)
- `/app/layout.tsx` (modify — add Google Font import)

**Required input:**
- Task 1.1 complete (`globals.css` and `layout.tsx` exist)

**Expected output:**

`/app/layout.tsx` imports the `Inter` font from `next/font/google` and applies it to the
root `<html>` element. The file must keep `export default` (required by Next.js — this is
the only permitted exception to the no-default-export rule):
```tsx
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'BIXBG Chatbot',
  description: 'Strumento interno per la qualificazione di esperienze BIXBG',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="it" className={inter.className}>
      <body>{children}</body>
    </html>
  );
}
```

`/app/globals.css` defines the three agent color tokens as CSS custom properties:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --color-compass: #6475FA;
  --color-architect: #E8650A;
  --color-bridge: #22C55E;
  --color-bg: #0f0f0f;
  --color-surface: #1a1a1a;
  --color-border: #2a2a2a;
  --color-text: #f0f0f0;
  --color-text-muted: #888888;
}

body {
  background-color: var(--color-bg);
  color: var(--color-text);
}
```

**Success criterion:**
Run `npm run dev`. Open `http://localhost:3000`. The page background must be dark
(`#0f0f0f`), not white. Open the browser DevTools → Elements → `<html>` — the class
attribute must contain the Inter font class (e.g., `class="__variable_... inter"`).

---

### Task 1.7 — Create the `AgentIndicator` component

**Files involved:**
- `/app/components/AgentIndicator.tsx` (create new)

**Required input:**
- Task 1.4 complete (`/app/components/` directory exists)
- Task 1.6 complete (CSS custom properties are defined)

**Expected output:**
`/app/components/AgentIndicator.tsx` is a named-export React component that accepts
`agentName: string` and `agentColor: string` as props and renders a colored badge
with the agent name. Exact file content:

```tsx
/**
 * AgentIndicator — displays the currently active agent's name and color as a badge.
 * @param agentName - Display name of the active agent (e.g. "BIXBG Compass")
 * @param agentColor - Hex color string for the agent (e.g. "#6475FA")
 */
export function AgentIndicator({
  agentName,
  agentColor,
}: {
  agentName: string;
  agentColor: string;
}) {
  return (
    <div
      className="flex items-center gap-2 px-3 py-1.5 rounded-full text-sm font-medium text-white"
      style={{ backgroundColor: agentColor }}
    >
      <span className="w-2 h-2 rounded-full bg-white opacity-80" />
      {agentName}
    </div>
  );
}
```

**Success criterion:**
Run `npx tsc --noEmit` from the project root. The command must complete with zero
TypeScript errors. The file must not use `export default`.

---

### Task 1.8 — Create the `MessageBubble` component

**Files involved:**
- `/app/components/MessageBubble.tsx` (create new)

**Required input:**
- Task 1.7 complete (confirms the component pattern is established)

**Expected output:**
`/app/components/MessageBubble.tsx` is a named-export component that accepts these props:
- `content: string` — the message text
- `role: 'user' | 'assistant'` — who sent the message
- `agentName?: string` — optional, display name of the agent (for assistant messages)
- `agentColor?: string` — optional, hex color for the left border accent

User messages align right with a neutral style. Assistant messages align left with a
colored left border using `agentColor`. Exact file content:

```tsx
/**
 * MessageBubble — renders a single chat message with role-appropriate styling.
 * @param content - The message text content
 * @param role - 'user' or 'assistant' — controls alignment and style
 * @param agentName - Display name shown above assistant messages
 * @param agentColor - Hex color applied as left border accent on assistant messages
 */
export function MessageBubble({
  content,
  role,
  agentName,
  agentColor,
}: {
  content: string;
  role: 'user' | 'assistant';
  agentName?: string;
  agentColor?: string;
}) {
  if (role === 'user') {
    return (
      <div className="flex justify-end mb-4">
        <div className="max-w-[70%] px-4 py-3 rounded-2xl rounded-tr-sm bg-[#2a2a2a] text-[#f0f0f0] text-sm leading-relaxed">
          {content}
        </div>
      </div>
    );
  }

  return (
    <div className="flex justify-start mb-4">
      <div
        className="max-w-[80%] px-4 py-3 rounded-2xl rounded-tl-sm bg-[#1a1a1a] text-[#f0f0f0] text-sm leading-relaxed border-l-4"
        style={{ borderColor: agentColor ?? '#6475FA' }}
      >
        {agentName && (
          <p
            className="text-xs font-semibold mb-1 opacity-70"
            style={{ color: agentColor ?? '#6475FA' }}
          >
            {agentName}
          </p>
        )}
        {content}
      </div>
    </div>
  );
}
```

**Success criterion:**
Run `npx tsc --noEmit`. Zero TypeScript errors. The file must not use `export default`.

---

### Task 1.9 — Create the `ChatInput` component

**Files involved:**
- `/app/components/ChatInput.tsx` (create new)

**Required input:**
- Task 1.8 complete (component pattern is established)

**Expected output:**
`/app/components/ChatInput.tsx` is a named-export component that accepts `disabled: boolean`
as a prop and renders a text input field and a send button. In this sprint, `disabled` is
always passed as `true` (hardcoded at call site in Task 1.11). The dynamic behavior
(disable while streaming) is implemented in Sprint 4. Exact file content:

```tsx
/**
 * ChatInput — text input and send button for the chat interface.
 * @param disabled - When true, both input and button are non-interactive.
 *   In Sprint 1 this is always true (hardcoded placeholder).
 *   Dynamic streaming-based disable/enable is implemented in Sprint 4.
 */
export function ChatInput({ disabled }: { disabled: boolean }) {
  return (
    <form className="flex gap-3 p-4 border-t border-[#2a2a2a] bg-[#0f0f0f]">
      <input
        type="text"
        disabled={disabled}
        placeholder={disabled ? 'Caricamento...' : 'Scrivi un messaggio...'}
        className="flex-1 px-4 py-3 rounded-xl bg-[#1a1a1a] border border-[#2a2a2a] text-[#f0f0f0] text-sm placeholder:text-[#888] focus:outline-none focus:border-[#6475FA] disabled:opacity-40 disabled:cursor-not-allowed transition-colors"
      />
      <button
        type="submit"
        disabled={disabled}
        className="px-5 py-3 rounded-xl bg-[#6475FA] text-white text-sm font-medium disabled:opacity-40 disabled:cursor-not-allowed hover:bg-[#5060e0] transition-colors"
      >
        Invia
      </button>
    </form>
  );
}
```

**Success criterion:**
Run `npx tsc --noEmit`. Zero TypeScript errors.
The file must not use `export default`.

---

### Task 1.10 — Create the `ChatWindow` component with hardcoded data

**Files involved:**
- `/app/components/ChatWindow.tsx` (create new)

**Required input:**
- Task 1.7 complete (`AgentIndicator` exists)
- Task 1.8 complete (`MessageBubble` exists)
- Task 1.9 complete (`ChatInput` exists)

**Expected output:**
`/app/components/ChatWindow.tsx` is a named-export component that assembles the full
chat interface using the three sub-components above. It uses hardcoded static data to
render one Compass welcome message. No state, no hooks, no API calls. Exact file content:

```tsx
import { AgentIndicator } from './AgentIndicator';
import { MessageBubble } from './MessageBubble';
import { ChatInput } from './ChatInput';

/** Hardcoded active agent for Sprint 1 static demo. Replaced by header-driven state in Sprint 4. */
const DEMO_AGENT = {
  name: 'BIXBG Compass',
  color: '#6475FA',
};

/** Hardcoded welcome message for Sprint 1 static demo. Replaced by dynamic messages in Sprint 4. */
const DEMO_MESSAGES = [
  {
    role: 'assistant' as const,
    content:
      'Ciao Matteo. Dimmi dell\'idea che hai in testa — anche se è ancora vaga, partiamo da lì.',
    agentName: DEMO_AGENT.name,
    agentColor: DEMO_AGENT.color,
  },
];

/**
 * ChatWindow — main chat container. Assembles AgentIndicator, message list, and ChatInput.
 * Sprint 1: renders static hardcoded data only.
 * Sprint 4: replaced with dynamic streaming state and header-driven agent switching.
 */
export function ChatWindow() {
  return (
    <div className="flex flex-col h-screen max-w-3xl mx-auto">
      {/* Header */}
      <div className="flex items-center justify-between px-6 py-4 border-b border-[#2a2a2a] bg-[#0f0f0f]">
        <h1 className="text-lg font-semibold text-[#f0f0f0]">BIXBG Chatbot</h1>
        <AgentIndicator agentName={DEMO_AGENT.name} agentColor={DEMO_AGENT.color} />
      </div>

      {/* Message list */}
      <div className="flex-1 overflow-y-auto px-6 py-6">
        {DEMO_MESSAGES.map((msg, index) => (
          <MessageBubble
            key={index}
            role={msg.role}
            content={msg.content}
            agentName={msg.agentName}
            agentColor={msg.agentColor}
          />
        ))}
      </div>

      {/* Input — hardcoded disabled in Sprint 1 */}
      <ChatInput disabled={true} />
    </div>
  );
}
```

**Success criterion:**
Run `npx tsc --noEmit`. Zero TypeScript errors. The file must not use `export default`.

---

### Task 1.11 — Update `page.tsx` to render `ChatWindow`

**Files involved:**
- `/app/page.tsx` (overwrite — replace the default Next.js welcome page content)

**Required input:**
- Task 1.10 complete (`ChatWindow` exists and compiles)
- Task 1.6 complete (`layout.tsx` is configured with font and metadata)

**Expected output:**
`/app/page.tsx` imports and renders `ChatWindow`. Per Next.js App Router requirements,
the page component uses `export default` (this is the only permitted exception to the
no-default-export rule). Exact file content:

```tsx
import { ChatWindow } from '@/app/components/ChatWindow';

/**
 * Root page — renders the BIXBG chat interface.
 * Uses export default as required by Next.js App Router.
 * All other files in this project use named exports.
 */
export default function Home() {
  return <ChatWindow />;
}
```

**Success criterion:**
Run `npx tsc --noEmit`. Zero TypeScript errors.

---

### Task 1.12 — Run the dev server and verify the full static UI

**Files involved:**
- None created — this is the sprint-level verification task

**Required input:**
- Tasks 1.1 through 1.11 all complete

**Expected output:**
A running dev server with the correct visual UI accessible at `http://localhost:3000`.

**Success criterion:**
Run `npm run dev`. Then open `http://localhost:3000` and verify ALL of the following —
each point must pass:

| Check | How to verify | Pass condition |
|---|---|---|
| Dev server starts | Terminal output | Shows `✓ Ready` with no errors or warnings |
| Page background is dark | Browser visual | Background is `#0f0f0f` (near-black), not white |
| Inter font is loaded | Browser DevTools → Network → Fonts | At least one `inter` font file appears |
| Agent badge is visible | Browser visual | A violet (`#6475FA`) rounded badge labeled "BIXBG Compass" appears in the header |
| Colored dot in badge | Browser visual | A small white circle appears to the left of "BIXBG Compass" inside the badge |
| Welcome message bubble | Browser visual | One message bubble appears with violet left border and the text starting with "Ciao Matteo" |
| Agent name above message | Browser visual | "BIXBG Compass" appears in small violet text above the message content |
| Text input is visible | Browser visual | An input field with placeholder "Caricamento..." is visible at the bottom |
| Send button is visible | Browser visual | A violet "Invia" button is visible next to the input |
| Input is disabled | Browser interaction | Clicking the input field does not allow typing |
| Button is disabled | Browser interaction | Clicking "Invia" does nothing (no form submission) |
| No console errors | Browser DevTools → Console | Zero red errors |

---

### Task 1.13 — Verify the security constraint: `.env.local` is not tracked by Git

**Files involved:**
- `/.env.local` (read only — must not appear in git tracking)
- `/.gitignore` (read only — must contain `.env.local`)

**Required input:**
- Task 1.3 complete (`.env.local` exists)
- Task 1.2 complete (`.gitignore` lists `.env.local`)

**Expected output:**
Confirmed proof that the API key placeholder file will never be committed.

**Success criterion:**
Run each command and confirm the expected output:
```bash
# 1. Confirm .gitignore rule is in place
grep "\.env\.local" .gitignore
# Expected: .env.local

# 2. Confirm git does not see the file
git status
# Expected: .env.local does NOT appear anywhere in the output.
# If it appears under "Untracked files" — .gitignore is not working. Stop and fix.

# 3. Confirm the file cannot be staged
git add .env.local 2>&1 || true
git status
# Expected: .env.local still does NOT appear under "Changes to be committed"
```
All three checks must pass. If any check fails, do not proceed to Sprint 2.

---

## Sprint 1 — Completion Checklist

Before marking Sprint 1 as complete, confirm every item:

- [ ] `npm run dev` starts without errors
- [ ] `http://localhost:3000` shows the dark-background BIXBG chat UI
- [ ] Violet `#6475FA` AgentIndicator badge for "BIXBG Compass" is visible in the header
- [ ] One message bubble from Compass is visible with violet left border
- [ ] Text input and "Invia" button are visible but non-interactive (hardcoded disabled)
- [ ] No console errors in the browser
- [ ] `npx tsc --noEmit` runs with zero errors
- [ ] `/knowledge-base/`, `/lib/`, `/prompts/`, `/app/components/` directories exist
- [ ] `next.config.js` exists with `outputFileTracingIncludes` for `knowledge-base/**/*.md`
- [ ] `.env.local` exists and is confirmed untracked by `git status`
- [ ] No file in `/app/components/` uses `export default`
- [ ] Only `page.tsx` and `layout.tsx` use `export default` (Next.js requirement)
